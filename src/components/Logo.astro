---
import logo from '../data/logo-paths.json';
---

<svg class="company-mark" viewBox={logo.viewBox} aria-hidden="true" focusable="false">
  <path d={logo.mark} fill="currentColor" />
  <path d={logo.dot} fill="var(--accent)" />
</svg>
