---
interface Props { items: { q: string; a: string }[] }
const { items } = Astro.props;
---
<div class="faq">
  {items.map(({ q, a }) => (
    <details>
      <summary>{q}</summary>
      <p>{a}</p>
    </details>
  ))}
</div>
